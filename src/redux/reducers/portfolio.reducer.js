
const initialState = {
  title: 'Portfolio',
};


// PORTFOLIO REDUCER
export function _portfolio(state = initialState, action = {}) {
  let newState = {...state };

  switch(action.type) {
    default:
      return newState;
  }
}



